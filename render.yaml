services:
  - type: web
    name: calming-echo
    buildCommand: npm run build
    startCommand: npm run start
    runtime: node
    region: oregon
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /api/health